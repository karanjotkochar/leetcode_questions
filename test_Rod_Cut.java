public class test_Rod_Cut {
    public static void main(String[] args) {
        int[] price = {1 , 5, 8, 9, 10, 17 , 17 , 20};

        maxValueRod(price);
        System.out.println(maxValueRod(price));
    }
    private static int maxValueRod(int[] price) {

        // Recursive solution
        int n = price.length;

        return 0;
    }
}
